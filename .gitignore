# Build outputs
/dist
/target
/src-tauri/target
/src-tauri/gen/

# Node
/node_modules

# Tauri-specta 자동 생성 (devmode 에서만)
/src/types/bindings.ts

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Env / 자격증명 (절대 커밋 금지 — CLAUDE.md §5)
.env
.env.local
.env.*.local
*.pem
*.key
*.pfx

# SSH 통합 테스트 픽스처의 런타임 생성 키 (테스트 전용 — 절대 커밋 금지)
/src-tauri/tests/fixtures/ssh/keys/

# Lock files for sub-projects (root 만 커밋)
# (pnpm-lock.yaml, Cargo.lock 은 root 에서 커밋)
.gstack/
